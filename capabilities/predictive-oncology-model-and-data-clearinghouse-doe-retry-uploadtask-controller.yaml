naftiko: 1.0.0-alpha2
info:
  label: MoDaC API — doe-retry-uploadtask-controller
  description: 'MoDaC API — doe-retry-uploadtask-controller. 1 operations. Lead operation: doe-retry-uploadtask-controller.
    Self-contained Naftiko capability covering one Predictive Oncology Model And Data Clearinghouse business surface.'
  tags:
  - Predictive Oncology Model And Data Clearinghouse
  - doe-retry-uploadtask-controller
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PREDICTIVE_ONCOLOGY_MODEL_AND_DATA_CLEARINGHOUSE_API_KEY: PREDICTIVE_ONCOLOGY_MODEL_AND_DATA_CLEARINGHOUSE_API_KEY
capability:
  consumes:
  - type: http
    namespace: predictive-oncology-model-and-data-clearinghouse-doe-retry-uploadtask-controller
    baseUri: http://localhost:8080
    description: MoDaC API — doe-retry-uploadtask-controller business capability. Self-contained, no shared references.
    resources:
    - name: uploadtask
      path: /uploadtask
      operations:
      - name: retryupload
        method: POST
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: taskId
          in: query
          type: string
          required: true
        - name: taskName
          in: query
          type: string
          required: true
  exposes:
  - type: rest
    namespace: predictive-oncology-model-and-data-clearinghouse-doe-retry-uploadtask-controller-rest
    port: 8080
    description: REST adapter for MoDaC API — doe-retry-uploadtask-controller. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/uploadtask
      name: uploadtask
      description: REST surface for uploadtask.
      operations:
      - method: POST
        name: retryupload
        description: retryupload
        call: predictive-oncology-model-and-data-clearinghouse-doe-retry-uploadtask-controller.retryupload
        with:
          taskId: rest.taskId
          taskName: rest.taskName
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: predictive-oncology-model-and-data-clearinghouse-doe-retry-uploadtask-controller-mcp
    port: 9090
    transport: http
    description: MCP adapter for MoDaC API — doe-retry-uploadtask-controller. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: retryupload
      description: retryupload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: predictive-oncology-model-and-data-clearinghouse-doe-retry-uploadtask-controller.retryupload
      with:
        taskId: tools.taskId
        taskName: tools.taskName
      outputParameters:
      - type: object
        mapping: $.
